# PRD: AI 학생상담 도우미 (AI Counseling Assistant)

- 문서 버전: v1.0
- 작성일: 2026-09-11
- 작성자: joy-lot
- 상태: Draft

---

## 1. 개요 (Overview)

### 1.1 배경
학교 현장의 교사는 학생 상담(학업, 교우관계, 진로 등)을 준비할 때 학생의 정보를 취합하고, 전문적이고 신중한 언어로 상담 멘트를 구성하는 데 상당한 시간을 쓴다. 또한 시험지·상담일지 같은 종이 문서는 디지털화되지 않아 데이터로 축적·분석되지 못하고, 학생별 성취수준 판단은 교사 개인의 경험에 크게 의존한다.

### 1.2 제품 정의
**AI 학생상담 도우미**는 교사가 학생의 기본 정보, 시험지 사진, 상담일지 사진을 입력하면, 온디바이스/로컬로 구동되는 LLM(Ollama Qwen)이 학교 내부 참고자료(RAG)를 근거로 다음을 자동 생성해주는 웹 서비스다.

1. 학생·학부모 상담용 전문 상담 멘트
2. 시험지 분석 기반 성취수준 진단 및 성적 향상 방향 멘트
3. 상담일지 분석 기반 고민 해결 방향 멘트

### 1.3 핵심 가치 제안
- **전문성**: 상담 전문 용어와 어조를 반영한 초안을 즉시 제공해 교사의 상담 준비 시간을 단축한다.
- **데이터 기반 판단**: OCR + RAG를 통해 학교 자체 평가 기준·상담 매뉴얼 문서를 근거로 일관된 성취수준 판단을 지원한다.
- **프라이버시 우선**: 민감한 학생 데이터를 다루므로 LLM 추론은 로컬 Ollama(Qwen)로 수행하고, 외부 상용 LLM API로 원문 데이터를 전송하지 않는다.

---

## 2. 목표 및 성공 지표

### 2.1 목표
- G1. 교사가 학생 기본정보 입력만으로 5초~30초 내 상담 멘트 초안을 받는다.
- G2. 시험지 사진 업로드 → OCR → 분석 → 성취수준/향상 방향 멘트까지 원스톱 처리.
- G3. 상담일지 사진 업로드 → OCR → 분석 → 고민 해결 방향 멘트까지 원스톱 처리.
- G4. 모든 AI 판단은 RAG로 학교 내부 문서(Documents)를 근거로 하여 "근거 없는 추측"을 최소화한다.
- G5. 클린 아키텍처 + TDD + 하네스 엔지니어링으로 유지보수 가능한 코드베이스를 확보한다.

### 2.2 성공 지표 (KPI)
| 지표 | 목표치 |
|---|---|
| 상담 멘트 생성 완료까지 걸리는 평균 시간 | ≤ 15초 |
| OCR 인식 성공률(시험지/상담일지) | ≥ 90% |
| 교사 재작성 없이 그대로 사용 가능하다고 응답한 비율 | ≥ 60% |
| 도메인/유스케이스 계층 테스트 커버리지 | ≥ 85% |
| RAG 근거 문서 인용 포함률(성취수준 판단 응답) | 100% |

---

## 3. 대상 사용자 (Personas)

1. **담임/교과 교사 (주 사용자)**: 학기 중 정기 상담, 학부모 상담을 준비. IT 숙련도는 보통 수준.
2. **상담(위클래스) 교사**: 상담일지를 지속적으로 기록·분석하는 전문 상담 인력.
3. **학년부장/관리자 (간접 사용자)**: 학생 데이터 접근 권한 관리, 통계 열람.

> 본 서비스는 학부모/학생 본인에게 직접 노출되지 않는 **교사 전용 내부 도구**를 1차 범위로 한다.

---

## 4. 핵심 기능 요구사항 (Functional Requirements)

### FR-1. 학생 기본정보 기반 상담 멘트 생성
- 입력: 학업성적(교과별 점수/등급 추이), 교우관계(또래관계, 갈등 이력, 사회성), 진로(희망 진로, 적성검사 결과), 기타 특이사항(자유 텍스트).
- 처리:
  - 입력 데이터를 구조화된 프롬프트로 변환.
  - RAG로 학교 상담 매뉴얼/평가 기준 문서에서 관련 근거 검색(top-k retrieval).
  - Ollama Qwen에 "학생 상담용" / "학부모 상담용" 두 가지 톤으로 각각 프롬프트 전달.
- 출력: 상담 멘트 초안(학생용/학부모용 분리), 근거로 사용된 문서 스니펫 및 출처 표기.
- 사용자는 생성된 멘트를 편집/저장/재생성(regenerate) 가능.

### FR-2. 시험지 사진 업로드 및 분석
- 입력: 스마트폰 촬영 시험지 이미지 (jpg/png/heic, 최대 10MB, 다중 페이지 지원).
- 처리:
  - 이미지 업로드 → Supabase Storage 저장.
  - OCR로 문항별 정답/오답, 배점, 단원 정보 추출(가능한 경우 정답지 매칭).
  - 추출 데이터를 근거로 단원별 성취수준(상/중/하 또는 성취기준 매핑) 산출.
  - RAG: 교과 성취기준 문서, 평가 루브릭 문서를 검색해 근거로 활용.
  - Qwen이 "성적 향상 방향" 멘트 생성 (구체적 취약 단원 + 학습 전략 포함).
- 출력: 단원별 성취수준 요약표, 취약점 리스트, 성적 향상 멘트, 근거 문서 인용.
- OCR 신뢰도가 낮은 문항은 "수동 확인 필요" 플래그 처리(품질 안전장치).

### FR-3. 상담일지 사진 업로드 및 분석
- 입력: 스마트폰 촬영 상담일지 이미지 (손글씨 포함 가능).
- 처리:
  - OCR(손글씨 인식 포함) → 텍스트 추출.
  - 개인정보/민감정보 마스킹 검토 단계(선택적 사람 확인).
  - RAG: 상담 사례집/위기유형별 대응 매뉴얼 문서 검색.
  - Qwen이 고민 주제 분류(교우관계/학업/가족/진로/정서 등) + 해결 방향 멘트 생성.
- 출력: 고민 주제 태그, 위험도(경/중/고) 표시(정서적 위기 신호 감지 시 경고), 해결 방향 상담 멘트, 근거 문서 인용.
- 고위험 신호(자해/폭력/방임 언급 등) 감지 시 AI 멘트 대신 **"전문기관 연계 필요"** 경고를 우선 노출 (안전장치, AI가 임의로 경도 있는 조언만 하지 않도록 함).

### FR-4. RAG 기반 문서 관리 (Documents)
- 관리자가 `Documents` 저장소에 PDF/DOCX/TXT 형태의 참고자료 업로드(교과 성취기준, 상담 매뉴얼, 평가 루브릭, 위기대응 지침 등).
- 업로드 시 청크 분할 → 임베딩 생성 → 벡터 스토어(Supabase pgvector) 저장.
- FR-1~3의 모든 AI 판단은 반드시 RAG 검색 결과를 프롬프트 컨텍스트에 포함해야 하며, 응답에는 참조한 문서명이 표시되어야 한다.
- 문서 버전 관리(재업로드 시 재인덱싱) 지원.

### FR-5. 학생/상담 기록 관리
- 학생 프로필 CRUD, 상담 이력 타임라인, 시험 성적 추이 조회.
- 생성된 멘트는 상담 기록으로 저장되어 이후 상담 시 이전 맥락을 참고(장기 컨텍스트).

### FR-6. 인증/권한
- 교사 계정 로그인(Supabase Auth).
- 학교/학년/반 단위 데이터 접근 범위 제한(RLS: Row Level Security).

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

| 구분 | 요구사항 |
|---|---|
| **프라이버시/보안** | 학생 개인정보는 국내 개인정보보호법(및 학교 내부 규정) 준수. LLM 추론은 로컬 Ollama로 수행하여 원문이 외부로 전송되지 않음. Supabase RLS로 반/학년 단위 접근 제어. |
| **성능** | 텍스트 기반 상담 멘트 생성 ≤ 15초, 이미지 OCR+분석 ≤ 40초(비동기 처리 + 진행 상태 표시). |
| **가용성** | Vercel 배포, Ollama 서버는 사내망/온프레미스 또는 별도 GPU 서버에서 구동(외부 노출 최소화). |
| **접근성** | WCAG 2.1 AA 수준 고려(교사의 스마트폰/데스크톱 혼용 사용 고려한 반응형 UI). |
| **감사 추적** | AI가 생성한 모든 멘트와 근거 문서는 로그로 저장되어 사후 검증 가능해야 함. |
| **테스트 가능성** | 클린 아키텍처 기반으로 도메인 로직은 프레임워크/외부 의존성 없이 단위 테스트 가능해야 함(TDD). |

---

## 6. 기술 스택 및 아키텍처 방향

### 6.1 기술 스택
- **프레임워크**: Next.js (App Router)
- **스타일링**: Tailwind CSS
- **애니메이션**: Framer Motion
- **아이콘**: Lucide Icons
- **배포**: Vercel
- **데이터베이스/스토리지/인증**: Supabase (Postgres + pgvector, Storage, Auth, RLS)
- **LLM**: Ollama 로컬 서빙, Qwen 2.5 계열 소형 모델(예: `qwen2.5:2b` 또는 사양에 맞는 근접 버전)
- **OCR**: 서버사이드 OCR 파이프라인(예: Tesseract 또는 온디바이스 대체 가능한 OCR 엔진) — 인쇄체/손글씨 이원화 처리
- **RAG**: 문서 청크화 + 임베딩(로컬 임베딩 모델 또는 Ollama 임베딩) + Supabase pgvector 유사도 검색

### 6.2 아키텍처 원칙
- **클린 아키텍처(Clean Architecture)**: Domain → Application(UseCase) → Interface Adapters → Infrastructure/Frameworks 4계층 분리. Next.js, Supabase, Ollama는 모두 Infrastructure 계층의 세부 구현으로 취급하며 도메인 계층은 이들에 의존하지 않는다.
- **TDD**: 도메인/유스케이스 계층은 테스트 우선으로 작성(Red-Green-Refactor). 외부 의존성(LLM, OCR, DB)은 인터페이스(Port)로 추상화하고 테스트에서는 Fake/Mock 구현체를 주입.
- **하네스 엔지니어링(Test Harness Engineering)**: LLM/OCR처럼 비결정적 출력을 갖는 컴포넌트를 검증하기 위한 전용 테스트 하네스 구축 — 골든 데이터셋 기반 회귀 테스트, 프롬프트 스냅샷 테스트, RAG 검색 품질 평가 하네스, Ollama 응답 재현을 위한 결정론적 Fake LLM 어댑터.

세부 아키텍처 규칙 및 폴더 구조는 [CLAUDE.md](./CLAUDE.md)에 정의한다.

---

## 7. 사용자 플로우 (User Flows)

### 7.1 상담 멘트 생성 플로우
1. 교사가 학생 검색/선택 → 학생 상세 페이지 진입.
2. "상담 멘트 생성" 탭에서 학업성적/교우관계/진로 입력 폼 작성(또는 기존 저장된 데이터 불러오기).
3. "생성" 클릭 → 로딩 애니메이션(Framer Motion) → RAG 검색 → LLM 호출.
4. 학생용/학부모용 탭으로 결과 표시, 근거 문서 배지 표시.
5. 편집 후 "상담 기록으로 저장" 또는 "다시 생성".

### 7.2 시험지 분석 플로우
1. "시험지 업로드" 진입 → 카메라 촬영 또는 파일 선택(모바일 최적화).
2. 업로드 진행률 표시 → OCR 처리 중 상태 표시.
3. OCR 결과 미리보기(신뢰도 낮은 항목 하이라이트) → 교사 확인/수정.
4. "분석하기" → 성취수준 리포트 + 향상 방향 멘트 표시.
5. 상담 기록에 연결 저장 가능.

### 7.3 상담일지 분석 플로우
1. "상담일지 업로드" → 촬영/업로드.
2. OCR 텍스트 확인 화면(민감정보 마스킹 옵션).
3. "분석하기" → 고민 주제 태그 + 위험도 배지 + 해결 방향 멘트.
4. 고위험 감지 시 경고 배너 우선 노출.
5. 상담 기록 저장.

---

## 8. 데이터 모델 개요 (Supabase)

- `students` (id, name, grade, class, created_at, ...)
- `student_academic_records` (student_id, subject, score, semester, ...)
- `student_relationship_notes` (student_id, note, category, created_at)
- `career_records` (student_id, aptitude_result, desired_path, ...)
- `exam_uploads` (id, student_id, image_url, ocr_raw_text, ocr_confidence, subject, created_at)
- `exam_analyses` (exam_upload_id, unit, achievement_level, weakness_summary, generated_comment)
- `counseling_log_uploads` (id, student_id, image_url, ocr_raw_text, created_at)
- `counseling_log_analyses` (log_upload_id, concern_category, risk_level, generated_comment)
- `counseling_comments` (id, student_id, type[student|parent], content, source_refs[], created_at, edited_by)
- `documents` (id, title, category, file_url, version, created_at)
- `document_chunks` (id, document_id, content, embedding vector, chunk_index)
- `teachers` (id, name, role, school_id, grade_scope, class_scope)

RLS 정책: `teachers.grade_scope`/`class_scope`에 속한 `students` 데이터만 조회 가능.

---

## 9. 안전장치 및 윤리적 고려사항

- AI는 **초안 제안자**일 뿐 최종 판단자가 아님을 UI 전반에 명시(면책 문구).
- 고위험(자해/학대/폭력 정황) 키워드 감지 시 AI 멘트 생성을 제한하고 전문기관 연계 안내를 우선 표시.
- 모든 생성 결과에 근거 문서 출처 표기(투명성).
- 민감 정보(주민등록번호, 연락처 등)는 OCR 결과에서 자동 마스킹 처리 시도.

---

## 10. 범위 외 (Out of Scope, v1)

- 학부모/학생 본인 직접 로그인 및 열람 기능.
- 다국어 지원.
- 클라우드 상용 LLM(OpenAI/Anthropic 등) 연동 — 로컬 Ollama만 사용.
- 실시간 화상 상담 기능.

---

## 11. 마일스톤 (제안)

1. **M0**: 클린 아키텍처 스캐폴딩 + 테스트 하네스 세팅 + Supabase 스키마.
2. **M1**: FR-1(텍스트 기반 상담 멘트 생성) end-to-end (RAG 포함).
3. **M2**: FR-2(시험지 OCR+분석).
4. **M3**: FR-3(상담일지 OCR+분석) + 안전장치(고위험 감지).
5. **M4**: 인증/권한(RLS), 상담 기록 관리 UI 고도화, Vercel 배포.
